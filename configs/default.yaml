defaults:

  - model: aegl

  - dataset: imagenet

  - optimizer: adamw

  - trainer: default

  - memory: default

  - graph: default

seed: 42

device: cuda

output_dir: outputs

log_dir: logs

checkpoint_dir: checkpoints